// Configuration for the frontend application
const API_HOSTNAME = 'localhost'; // Assuming backend runs locally
const API_PORT = 5001;
const WS_PROTOCOL = 'ws:'; // Use 'wss:' for secure WebSocket
// Use the actual key from the .env file
const BACKEND_KEY = 'f3b1d4e8a9c0f2e5d8a3b1c4e7f9d0a2'; 

// Define Environment-specific settings (can be adjusted)
const ENV_CONFIG = {
  // ... existing code ...
}; 